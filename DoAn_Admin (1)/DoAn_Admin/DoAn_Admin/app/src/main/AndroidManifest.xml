<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doan_admin1">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoAn_Admin">

        <!-- Màn hình login (launcher) -->
        <activity
            android:name=".activity.AdminLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Màn hình chính admin -->
        <activity
            android:name=".activity.AdminMainActivity"
            android:exported="false" />

        <!-- Màn hình quản lý người dùng -->
        <activity
            android:name=".activity.AdminUserActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AdminSosActivity"
            android:exported="false"/>


        <!-- Thêm các activity khác nếu cần -->
        <!--
        <activity android:name=".activity.AdminSettingsActivity" android:exported="false" />
        -->
    </application>

</manifest>
